node_modules/
dist/
.mcpgaze/
*.log
.DS_Store
native/**/target/
Cargo.lock
# Cassettes can capture request params / response results verbatim — don't
# commit them by default (record with --redact, or curate before sharing).
mcpgaze.cassette.json
*.cassette.json
